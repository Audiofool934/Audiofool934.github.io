---
const { title, url, description, image} = Astro.props
---
<li>
    <a href={url}>
        <h2>{title}</h2>
        <!-- <p>{description}</p> -->
        {image && (
            <img src={image.url} alt = {image.alt} loading="lazy" style="max-width: 400px; height: auto;" />
        )}
    </a>
</li>